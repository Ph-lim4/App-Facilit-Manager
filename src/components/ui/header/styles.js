import styled from 'styled-components/native';
import { h } from '../../dimens';

export const TopView = styled.View`
    margin-top: ${h(42)}px;
    margin-bottom: ${h(35)}px;
    flex-direction: row;
    align-items: center;
`;
